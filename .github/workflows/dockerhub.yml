name: Build & Push to Docker Hub (CLI)

on:
  push:
    branches: ["master"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify token present
        env:
          TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          if [ -z "$TOKEN" ]; then
            echo "ERROR: DOCKERHUB_TOKEN is missing"
            exit 1
          fi
          echo "OK: token present"

      - name: Docker login
        env:
          TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: echo "$TOKEN" | docker login -u joan30122001 --password-stdin

      - name: Build image
        run: docker build -t joan30122001/jira:latest .

      - name: Push image
        run: docker push joan30122001/jira:latest
